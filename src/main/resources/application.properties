# Application
spring.application.name=bismuto-user-admin-service
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://jesus-pinzon:<user_name>@<database_name>.dmssz.mongodb.net/
spring.data.mongodb.database=eci_bienestar_users

# Logging
logging.level.org.springframework=INFO
logging.level.edu.eci.cvds.bismuto.auth=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Security (adapt when implemented)
# app.jwt.secret=your-secret-key
# app.jwt.expiration-ms=86400000

# Actuator (optional, for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
